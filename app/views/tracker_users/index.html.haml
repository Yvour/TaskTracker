%h1 Listing tracker_users

%table
  %tr
    %th Email
    %th Is admin
    %th
    %th
    %th

  - @tracker_users.each do |tracker_user|
    %tr
      %td= tracker_user.email
      %td= tracker_user.is_admin
      %td= link_to 'Show', tracker_user
      %td= link_to 'Edit', edit_tracker_user_path(tracker_user)
      %td= link_to 'Destroy', tracker_user, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Tracker user', new_tracker_user_path
