%h1 Listing tracker_projects

%table
  %tr
    %th Name
    %th Description
    %th
    %th
    %th

  - @tracker_projects.each do |tracker_project|
    %tr
      %td= tracker_project.name
      %td= tracker_project.description
      %td= link_to 'Show', tracker_project
      %td= link_to 'Edit', edit_tracker_project_path(tracker_project)
      %td= link_to 'Destroy', tracker_project, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Tracker project', new_tracker_project_path
